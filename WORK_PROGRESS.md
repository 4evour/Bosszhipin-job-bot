# 项目进度摘要

更新时间：2026-07-05

本文档记录当前仓库的功能进度，便于后续维护和新会话接手。这里不记录个人招呼语、平台账号状态、密钥或本地运行日志。

## 已完成

- GUI 主入口：保留 CLI 给高级用户和测试使用，日常操作以 Tauri GUI 为主。
- 固定招呼语：移除 LLM/RAG、简历解析和自动生成招呼语链路，运行时只使用用户上传或填写的固定招呼语。
- Profile 配置：岗位规则、页面筛选、发送节奏、Dry-run、最大发送数等都由 profile 管理。
- 关键词规则：支持 `contains`、`regex`、`exact`，支持只查岗位名称、只查正文、岗位名称加正文等匹配范围。
- 必需关键词：例如“实习”不再写死，用户可以在 GUI 中配置。
- 页面筛选：城市、学历等页面筛选保留尽力点击；失败时警告并继续，由本地规则兜底。
- BOSS 活跃状态：改为从岗位文本中本地提取，并按用户选择的多个状态做 OR 判断。
- 手动筛选流程：GUI 默认复用当前受控 BOSS 页面，用户可先在网页端筛到目标列表，再启动扫描或发送。
- 岗位遍历：优先滚动左侧岗位列表，兼容 BOSS 虚拟列表只保留固定数量岗位卡片的情况。
- 发送后返回：发送招呼语跳转聊天页后，会尝试返回岗位列表继续处理。
- 日志增强：进度事件带本地时间戳，GUI 运行日志显示中文通过/跳过原因。
- 结果记录：`sent_jobs.jsonl` 记录 `match_explanation`，便于复盘为什么通过。

## 当前重点

- 继续验证左侧岗位列表滚动在真实 BOSS 页面上的稳定性。
- 继续优化 GUI 运行事件的可读性，让非技术用户能看懂每条岗位为什么通过或跳过。
- 保持 Dry-run 与真实发送共用同一套筛选和遍历逻辑，降低真实发送前的验证成本。

## 注意事项

- 不要提交个人招呼语、简历 PDF、平台日志、Chrome profile、API key 或本地测试 profile。
- 真实发送前建议先使用 Dry-run 扫描，确认关键词、匹配范围、活跃状态和发送节奏。
- 如果 BOSS 页面结构变化，优先从 `src/boss_zhipin/website_oper/finding_jobs.py` 的岗位卡片解析和滚动逻辑排查。
